# RAG Chatbot - Requirements
# Install with: pip install -r requirements.txt

# Core UI
streamlit>=1.32.0

# Document Loading
pymupdf>=1.24.0          # PDF loading (fitz)

# Vector Store
chromadb>=0.5.0          # Local persistent vector DB

# Embeddings & LLM (via Ollama REST API)
requests>=2.31.0         # HTTP calls to Ollama

# Numerical operations
numpy>=1.26.0            # Cosine similarity fallback

# Utilities
pathlib2>=2.3.7          # Path handling (stdlib on Python 3.4+)
