# RAG Chatbot - Requirements
# Install with: pip install -r requirements.txt

# Core UI
streamlit>=1.32.0

# Document Loading
pymupdf>=1.24.0          # PDF loading (fitz)

# Vector Store
# (ChromaDB removed for ephemeral cloud deployment; using in-memory numpy)

# Embeddings & LLM
groq>=0.5.0              # Cloud LLM Provider
sentence-transformers>=2.7.0 # Local Embeddings (replaces Ollama embeddings)
python-dotenv>=1.0.0     # Environment variable loading

# Numerical operations
numpy>=1.26.0            # Cosine similarity fallback

# Utilities
pathlib2>=2.3.7          # Path handling (stdlib on Python 3.4+)
